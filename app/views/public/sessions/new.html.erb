<div class="container">
  <div class="row">
    <div class="mx-auto">

    <h2 class="mt-5">ログイン</h2>
    
    <h4 class="mt-3">会員の方はこちらからログイン</h4>
    
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field">
        <%= f.label :メールアドレス, class: "mt-3 mr-5" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "mb-2 ml-3" %>
      </div>
    
      <div class="field">
        <%= f.label :パスワード, class: "mr-5" %>
        <%= f.password_field :password, autocomplete: "current-password", class: "mb-2 mx-5" %>
      </div>
    
      <div class="actions">
        <%= f.submit "ログイン", class: "btn btn-outline-success mb-2" %>
      </div>
    <% end %>
    
    <h3 class="mt-5 mb-2">会員登録がお済みでない方</h3>
    
    <%= link_to "こちら", new_customer_registration_path %>
    から新規登録を行ってください。
   </div>
  </div>
</div>